<?php

namespace Tests\Feature;

use Tests\TestCase;
use Illuminate\Foundation\Testing\WithFaker;
use Illuminate\Foundation\Testing\RefreshDatabase;

class HomePageTest extends TestCase
{
    /**
     * A basic test example.
     *
     * @return void
     */
    public function testRegistration()
    {   
        // $this->get('/register')
        // ->type('Me', 'name')
        // ->type('someone@outlook.com', 'email')
        // ->type('secret', 'password')
        // ->type('secret', 'password_confirmation')
        // ->press('Register')
        // ->see('register')
        // ->seeInDatabase('users', ['email' => 'someone@outlook.com']);

        // $this->get('/student/register')
        // ->type('Name', 'Someone Else')
        // ->type('CPF', '123')
        // ->type('RG', '123')
        // ->type('Address', '123')
        // ->type('Cellphone', '123')
        // ->press('Register')
        // ->seeInDatabase('students', ['Name' => 'Someone Else']);

        // $response = $this->get('/register');

        // $response->assertType('Me', 'Name');
        // $response->assertType('someone@outlook.com', 'email');
        // $response->assertType('secret', 'password');
        // $response->assertType('secret', 'password_confirmation');
        // $response->assertPress('Register');
        // $response->seeInDatabase('users', ['email' => 'someone@outlook.com']);
    
    }

    
}
